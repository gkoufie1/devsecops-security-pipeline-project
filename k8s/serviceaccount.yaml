apiVersion: v1
kind: ServiceAccount
metadata:
  name: devsecops-demo
  namespace: devsecops
  annotations:
    # Replace ACCOUNT_ID and configure IRSA before deploying to production
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/devsecops-demo-role
